export enum MessageDirectionEnum {
  INCOMING = 'incoming',
  OUTGOING = 'outgoing',
}

export enum AuthorEnum {
  EXTERNAL = 'external',
  EMPLOYEE = 'employee',
  BOT = 'bot',
}

export enum AmoEntityEnum {
  LEAD = 'lead',
  CONTACT = 'contact',
  COMPANY = 'company',
}

export enum AmoElementEnum {
  LEAD = 2, // Сделка
  CONTACT = 1, // Контакт
  COMPANY = 3, // Компания
}
